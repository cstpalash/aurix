# Core Framework
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP Client
httpx>=0.26.0

# AI - Code Review (cost-effective: ~$0.01/review with gpt-4o-mini)
openai>=1.10.0

# CLI & Output
rich>=13.7.0
typer>=0.9.0

# Configuration
pyyaml>=6.0.0

# Testing (dev only)
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Optional: API Server (install with `pip install -e ".[server]"`)
# fastapi>=0.109.0
# uvicorn>=0.27.0
